language: php
sudo: true # Use modern Travis builds – http://docs.travis-ci.com/user/migrating-from-legacy/

notifications:
    on_success: never
    on_failure: change

php:
    - 5.6

env:
    # latest stable:
    - WP_VERSION=latest WP_MULTISITE=0

install:
    - bash bin/install.sh

before_script:
    - php -v
    - which php
    - bash bin/install-wp-tests.sh wordpress_test root '' localhost $WP_VERSION

script:
    - bash bin/script.sh
